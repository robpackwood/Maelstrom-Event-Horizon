# Executable builds

`win-x64/MaelstromEventHorizon.exe` is a self-contained, single-file Windows release built from the repository source.

Run it directly on 64-bit Windows. The .NET runtime, artwork, music, and sound assets are included in the executable.
